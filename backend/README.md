# CameraCommander Backend

Python FastAPI backend for controlling CameraCommander pan/tilt stepper head & Canon DSLR over serial and gphoto2.

## Quickstart with `uv`

```bash
# Sync dependencies and create venv
uv sync

# Run backend server
uv run python main.py
```
